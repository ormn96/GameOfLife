import { FilterRegexPipe } from './filter-regex.pipe';

describe('FilterRegexPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterRegexPipe();
    expect(pipe).toBeTruthy();
  });
});
